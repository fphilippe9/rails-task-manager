 <% @tasks.each do |task|%>
<h2><input type="checkbox"><%= link_to task.title, task_path(task) %></h2>


<%= link_to "Delete", task_path(task),
      method: :delete,
      data: { confirm: "Are you sure?" } %>
      <% end %>


<%= link_to "Add a new task", new_path %>
